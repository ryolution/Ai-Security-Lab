# Lab Template

This lab contains an intentionally vulnerable implementation and a secured implementation for one manually verified vulnerability.

Expected behavior:

- `vulnerable/app.py`: attack succeeds and the vulnerability test fails
- `secured/app.py`: attack is blocked and the vulnerability test passes

Use only synthetic data and local fixtures.
