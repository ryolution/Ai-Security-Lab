# Vulnerabilities

This folder contains all vulnerability categories.

Add a new vulnerability by copying:

```text
template/vulnerability/
```

into one category folder, for example:

```text
vulnerabilities/rag/example-vulnerability-name/
```
